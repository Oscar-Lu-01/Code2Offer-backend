0：docker配置elasticsearch、kibama、hotkey、sentinal、nacos

//停止并删除所有服务
docker stop $(docker ps -aq)
docker rm $(docker ps -aq)
//重启某个服务
docker-compose restart mysql


# 启动所有服务，-d 表示后台运行
docker compose -f docker-compose-Code2Offer.yml up -d
docker compose -f docker-compose-Code2Offer.yml up -d --build
docker compose -f docker-compose-Code2Offer.yml down
docker compose -f docker-compose-Code2Offer.yml logs -f nacos




2、流控的配置以及写法都有问题，流量控制从配置中心拉取


1、ES降级：es挂掉不影响搜索






spirngboot流程
1、java启动
2、spring上下文初始化
    加载bootstrap.yml 废弃
    加载application.yml
3、创建bean（注解实际是继承关系）

hotkey中用到的etcd
充当注册中心：（存储状态、心跳保活、通知机制）
1、worker的ip不用写死，只需要写死注册中心的id
2、节点挂掉之后通过心跳统计
3、规则修改同步，不然会导致集群中配置不同步


http://localhost:8101/api/doc.html

http://106.13.184.117:8848/nacos
es
http://106.13.184.117:5601/app/home